// Simulación: sincroniza cada 15 min (900000 ms)
setInterval(() => {
  console.log("🔄 Sincronizando con GitHub...");
  // Aquí iría la conexión a la API de GitHub para subir/descargar calendar.json
}, 900000);
